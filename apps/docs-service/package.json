{
  "name": "@korean-erp/docs-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('No tests yet for docs-service')\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pdfkit": "^0.17.1"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.3",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
