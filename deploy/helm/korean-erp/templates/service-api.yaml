apiVersion: v1
kind: Service
metadata:
  name: {{ include "korean-erp.fullname" . }}-api
  labels:
    {{- include "korean-erp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  selector:
    app.kubernetes.io/component: api
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
